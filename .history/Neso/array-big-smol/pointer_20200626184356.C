#include <stdio.h>
#include <stdlib.h>
//exercise to find the largest and smallest element in an array 
int main() {
    int a[]= {23,45,6,98};
    int min,max,i;
    min = max = a[0];
    //if i is smaller than min
    for(i=0; i<min; i++) {
        
    }
    return 0;
}